mixin range(id, disabled=false, step=1, max=1, min=1)
    label.form-label(for=id)
        block
    input.form-range(type="range", id=id, disabled=disabled, step=step, max=max, min=min)&attributes(attributes)