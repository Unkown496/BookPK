include ../mixins/components 

block vars
    - 
        let title = ""

doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title=title
    body.vh-100.w-100.d-flex.flex-column
        +header()

        main.flex-fill
            block main 
        
        +footer()